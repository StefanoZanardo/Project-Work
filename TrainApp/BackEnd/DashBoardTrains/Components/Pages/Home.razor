@page "/"
@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        // Quando si apre il sito per la prima volta, vai subito alla pagina di login
        NavManager.NavigateTo("/login", forceLoad: false);
    }
}
